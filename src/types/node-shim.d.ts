// Minimal stubs for browser-only types referenced in shared domain models.
// The CLI does not instantiate these; they exist only for structural type compatibility.

declare interface HTMLCanvasElement {
  width: number;
  height: number;
}
