{
  "name": "lutchainer",
  "version": "1.0.0",
  "description": "Browser-based LUT chaining shader editor with WebGL preview",
  "license": "CC0-1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "serve": "node scripts/serve.mjs",
    "validate:lutchain-examples": "node scripts/validate-lutchain-examples.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "esbuild-plugin-solid": "^0.6.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "solid-js": "^1.8.17"
  }
}
